{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++ build active file",
            "command": "/usr/bin/g++",
            "args": [                
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "-Iinclude",
                "-Isrc",
                "-g",
                "${fileDirname}/${fileBasename}",
                "${fileDirname}/src/ann/xtensor_lib.cpp",
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        },
        {
            "label": "Remove Executable",
            "type": "shell",
            "command": "rm",
            "args": [
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "problemMatcher": []
        },
        {
            "label": "Check memory leaks",
            "type": "shell",
            "command": "valgrind",
            "args": [
                "--leak-check=full",
                "--show-leak-kinds=all",
                "--track-origins=yes",
                "--verbose",
                "--log-file=${fileDirname}/valgrind-out.txt",
                "--quiet",
                "${fileDirname}/${fileBasenameNoExtension}","8","20"
            ],
        }
    ],
    "version": "2.0.0"
}

